ig.module(
	'game.entities.warp'
)
.requires(
    'impact.entity',
    'impact.debug.debug',
    'game.entities.player'
)
.defines(function(){
EntityWarp = ig.Entity.extend({
    // Class Declaration stuff goes in here
})
})

