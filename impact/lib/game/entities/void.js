ig.module(
	'game.entities.void'
)
.requires(
    'impact.entity',
    'impact.debug.debug',
    'game.entities.player'
)
.defines(function(){
EntityVoid = ig.Entity.extend({
    // Class Declaration stuff goes in here
})
})